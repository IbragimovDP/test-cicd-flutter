name: testcicd
description: test cicd with flutter

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: 2.10.5

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_screenutil: 5.5.3+2
  firebase_core: 1.17.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
